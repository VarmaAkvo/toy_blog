<div class="col-2">
  <div class="row card mb-2">
    <div class="card-body d-flex flex-column justify-content-center align-items-center">
      <%= avatar_of(@owner, 96) %>
      <h5 class="card-title mb-1"><%= @owner.name %></h5>
      <div class="mb-2">
        <% if current_user && current_user.following?(@owner) %>
          <%= link_to '取消关注', '#', method: :delete, class: 'btn btn-danger btn-sm py-0' %>
        <% else %>
          <%= link_to '关注', '#', method: :post, class: 'btn btn-primary btn-sm py-0' %>
        <% end %>
      </div>
      <div class="card-text small"><%= @owner.profile %></div>
      <%= tag_list(@owner.tags) %>
    </div>
  </div>
  <div class="row card">
    <div class="card-header text-white bg-primary">该博客的tag检索</div>
    <div class="card-body">
      <%= tag_list_with_count(@uats) %>
    </div>
  </div>
</div>
